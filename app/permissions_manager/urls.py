# from django.urls import path
# from .views import ApplicationCreateView, GroupCreateView, PermissionCreateView

# app_name = 'permissions_manager'  # Namespace for app-specific URLs

# urlpatterns = [
#     path('add-application/', ApplicationCreateView.as_view(), name='application-add'),
#     path('add-group/', GroupCreateView.as_view(), name='group-add'),
#     path('add-permission/', PermissionCreateView.as_view(), name='permission-add'),
#     # Add more URL patterns for other views if needed
# ]