build:
  docker:
    web: Dockerfile
#release:
#  command:
#    - "./docker-entrypoint.sh migrations"
#  image: web
run:
  web: "" #./docker-entrypoint.sh
  worker:
    command:
      - worker
#      - "./docker-entrypoint.sh worker"
    image: web
  scheduler:
    command:
      - scheduler
#      - "./docker-entrypoint.sh scheduler"
    image: web