build:
  docker:
    web: Dockerfile
release:
  command:
    - migrations
  image: web
run:
  web: ./docker-entrypoint.sh
  worker:
    command:
      - ./docker-entrypoint.sh worker
    image: web
  scheduler:
    command:
      - ./docker-entrypoint.sh scheduler
    image: web